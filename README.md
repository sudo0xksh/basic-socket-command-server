# basic-socket-command-server
A simple Python TCP server that accepts commands and sends back responses.  Built to understand socket communication, not to bypass security.  Straight input, straight output.
